<% if @strategy_items.any? %>
  <ol class="strategies">
    <%= render @strategy_items %>
  </ol>
  <%= will_paginate @strategy_items,
                    params: { controller: :static_pages, action: :home } %>
<% end %>